server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medico_db
    username: root
    password: TU_PASSWORD

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true